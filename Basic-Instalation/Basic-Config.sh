cp /etc/hostname /etc/hostname-bkp && echo kavosh-db-mysql-3 > /etc/hostname && echo kavosh-db-mysql-3 > /etc/hostname && cp /etc/hosts /etc/hosts-bkp && sed -i 's|manage|kavosh-db-mysql-3/|g' /etc/hosts &&  timedatectl set-timezone Asia/Beirut ; timedatectl set-ntp false ; timedatectl set-ntp true ; systemctl restart systemd-timesyncd.service && reboot
cp /etc/hostname /etc/hostname-bkp && echo kavosh-db-mongo-1 > /etc/hostname && echo kavosh-db-mongo-1 > /etc/hostname && cp /etc/hosts /etc/hosts-bkp && sed -i 's|manage|kavosh-db-mongo-1/|g' /etc/hosts &&  timedatectl set-timezone Asia/Beirut ; timedatectl set-ntp false ; timedatectl set-ntp true ; systemctl restart systemd-timesyncd.service && reboot
cp /etc/hostname /etc/hostname-bkp && echo kavosh-db-postgres-1 > /etc/hostname && echo kavosh-db-postgres-1 > /etc/hostname && cp /etc/hosts /etc/hosts-bkp && sed -i 's|manage|kavosh-db-postgres-1/|g' /etc/hosts &&  timedatectl set-timezone Asia/Beirut ; timedatectl set-ntp false ; timedatectl set-ntp true ; systemctl restart systemd-timesyncd.service && reboot
cp /etc/hostname /etc/hostname-bkp && echo kavosh-keycloak > /etc/hostname && echo kavosh-keycloak > /etc/hostname && cp /etc/hosts /etc/hosts-bkp && sed -i 's|manage|kavosh-keycloak/|g' /etc/hosts &&  timedatectl set-timezone Asia/Beirut ; timedatectl set-ntp false ; timedatectl set-ntp true ; systemctl restart systemd-timesyncd.service && reboot